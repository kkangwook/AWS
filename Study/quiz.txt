cd이동
디렉토리만들기 -> 안에 파일만들기 -> 카피하기 -> 이동시키기 -> 파일지우기 ->디렉토리지유기  
그림파일 무슨 확장자인지
로그관련파일찾기
text.txt에 아무거나 넣기 3가지 방법(echo, cat, vi) -> 페이지단위로 보기
grep (뒤에 다른문제있지않을까?) pid찾기?
date날짜 나타내는법: date +%y-%m-%d-%H-%M-%S
sed
awk
백그라운드로실행->pid찾기->죽이기
쉘리스트보고->변경->확인
변수만들고 목록으로보고 제거하기
주요환경변수들 -> 환경변수등록 코드는?
alias만들고 지우기
2>> /dev/null문제 ->.bashrc어디있는지 찾는법
du /bin/ -> sort -> 상위7개
로그인한 사용자 계정명이 있는 행만 출력
사용자 계정으로 실행한 프로세스를 모두 출력
위치매개변수 쉘 만들고 제목,각변수, 개수, 리스틑로 출력되게 + chmod까지도 + bc도
섭씨온도 입력받아 화씨온도return하는 스크립트짜기
파일이름입력받아 존재유무로 exists or nope 출력하는 스크립트 짜기 두가지 방법(test,  [ ~ ] )
숫자입력받아 100보다 크거나 2의배수이면 hi, 100보다작고 10보다크면 good, 그외는 bye출력
	이때 위에 cat<<END ~ END로 위의 조건 각 줄별로 입력 + 그 외시 exit 1 -> $?로 1나오는지 확인
while문: number: 1 ~ number: 5까지 출력하고 모든 값 더한값 마지막에 출력
while+if로 1부터 10까지 수를 출력하고, 홀수인지 짝수인지 구분
for문 + seq로 1~10출력후 모든수 더한값 출력
for문으로 입력한 모든 매개변수의 합을 구하는 쉘 프로그램을 작성하시오.(순차적으로 더해진값 줄마다 출력)
